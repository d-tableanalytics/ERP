# Copy this file to .env and fill in your values.
# Leave this blank to call the same origin as the app.
# For local development, Vite proxies /api and /uploads to http://localhost:5000.
# In production, set this only if the API is hosted on a different public HTTPS origin.

VITE_API_BASE_URL=

